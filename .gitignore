node_modules
dist
.DS_Store
*.log
.env
.env.*
!.env.example

# Local working docs (Claude scratchpad)
.claude/
.cursor/
CLAUDE.md

# Local-only worktree data — never commit these
public/worktree-data.json
public/worktree-docs.json
